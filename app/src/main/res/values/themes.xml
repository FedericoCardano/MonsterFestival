<resources>
    <!-- Base application theme. -->
    <style name="Theme.MonsterFestival" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/brawnColor</item>
        <item name="colorSecondaryVariant">@color/grigio</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Text color -->
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:textColorSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorOnSecondary</item>
        <!-- Customize your theme here. -->
        <item name="android:windowFullscreen">false</item> <!-- The StatusBar is deleted -->
    </style>

    <style name="roundedImageViewRounded">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>
    <style name="RatingBar" parent="Theme.AppCompat">
        <item name="colorControlNormal">@color/grigio</item>
        <item name="colorControlActivated">@color/giallo</item>
    </style>


</resources>